def delete_review(customerId,businessId):
    target = filter/query('reviews').filter_by(businessId == businessId,customerId == customerId)
    db.delete(target)
    return message,status_code


def update_review(customerId,businessId)
    data = request.json
    target = filter/query('reviews').filter_by(businessId == businessId,customerId == customerId)
    ReviewModel.update(target,new_data)
    return message,status_code
